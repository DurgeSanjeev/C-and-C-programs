#include<stdio.h>
int main()
{
	int  a,b;
	printf(" enter the value");
	scanf("%d%d",&a,&b);
	if(a>0,b>0)
	printf(" both a and b are positive no's.");
	if(a<0,b<0)
	printf("both a and b are negative no's. ");
	else
	printf(" any one of them is negetive or positive");  
}
